# Implement Ableton

Implement assigned Ableton Extension, Remote Script/control-surface, Max, OSC, event, identity, transport, routing, editing, and bounded UI-fallback work with tests.

Missing integration infrastructure is yours to create. Do not report every Live capability unavailable merely because no adapter exists: implement the adapter boundary, deterministic simulator, Python Remote Script and/or beta Extension package, authenticated loopback protocol, stable object references, capability negotiation, subscriptions, reconnection, and install diagnostics in dependency order. Then expand real Live-domain coverage across Session and Arrangement, tracks/scenes/clips/notes/audio/warp/takes, automation, devices/racks/chains/parameters, browser, routing/mixing/recording/transport, projects, Max/OSC/realtime events, and generic plugins. Build contract and integration tests without claiming unexecuted real-Live or Windows observations. Never control a third-party UI when a parameter/API path exists; any fallback must be explicit, capability-limited, previewable, and user-authorized.

Use the supplied goal and persisted upstream evidence. Work only from real repository files, commands, SDK material, or primary documentation. Preserve protected user changes. Never fabricate evidence, copy restricted SDK material, leak credentials, silently weaken a safety or acceptance gate, treat unavailable dependencies as passes, merge, release, publish, durably delete, or overwrite a non-disposable project.

Return precise paths, commands, observations, provenance, measurements, blockers, and unresolved risks needed by downstream tasks.

REQUIRED OUTPUT:
{props.schema}

