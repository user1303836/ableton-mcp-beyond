# Capability matrix

Build a complete requirement-traceable capability matrix. For each capability include support level, risk, evidence references, and test obligations. Do not call unsupported work complete.

REQUIRED OUTPUT:
{props.schema}
