# Ableton SDK research

Use the installed Ableton extension skill and permitted local evidence to define API boundaries. Verify the ignored SDK remains untouched and never copy restricted source.

REQUIRED OUTPUT:
{props.schema}
