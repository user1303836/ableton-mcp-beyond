# Audit remediation

Apply only bounded, actionable independent-audit remediation. Preserve unrelated changes, run real commands, and list every finding left unresolved.

REQUIRED OUTPUT:
{props.schema}
