# Architecture design

Design components, contracts, serialization rules, durable checkpoints, and recovery behavior within the existing repository boundary. Include implementation limits and residual risks.

REQUIRED OUTPUT:
{props.schema}
