# PR and baseline research

Inspect PR #1, branch history, CI conventions, test topology, and unrelated seeded Smithers failures. Preserve immutable identifiers and scope claims to evidence.

REQUIRED OUTPUT:
{props.schema}
