# Audit moderation

Deduplicate independent audit findings, assign severity, identify evidence gaps, and produce bounded remediation IDs. Do not downgrade unresolved critical risks.

REQUIRED OUTPUT:
{props.schema}
