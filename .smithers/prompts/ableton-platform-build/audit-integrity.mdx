# Integrity audit

Independently audit implementation integrity, test evidence, branch and checkpoint state, PR state, runtime shape, UI behavior, and ignored-SDK protection.

REQUIRED OUTPUT:
{props.schema}
