# Test strategy

Design real unit, contract, property, integration, performance, end-to-end, UI, render, runtime-shape, and focused TypeScript checks. Isolate known baseline failures.

REQUIRED OUTPUT:
{props.schema}
