# Validation fix

Apply one bounded ScanFixVerify pass for focused failures only. Do not mask unrelated baseline failures. Run real checks and preserve exit evidence.

REQUIRED OUTPUT:
{props.schema}
