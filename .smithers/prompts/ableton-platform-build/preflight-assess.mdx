# Preflight assessment

Assess the real preflight row. Preserve assumptions and the explicit boundary that this repair does not launch the Ableton build. Escalate only when recovery cannot be deterministic.

REQUIRED OUTPUT:
{props.schema}
