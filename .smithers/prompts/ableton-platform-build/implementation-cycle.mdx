# Implementation cycle

Implement the approved bounded iteration on feat/comprehensive-ableton-mcp. Do not touch the ignored SDK, launch Ableton, or exceed the approved boundary. Run real commands and report evidence.

REQUIRED OUTPUT:
{props.schema}
