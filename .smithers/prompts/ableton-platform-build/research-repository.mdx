# Repository research

Inspect the repository, existing workflow, UI, scripts, tests, and baseline failures without editing. Record commands, paths, provenance, requirements, defects, and unknowns.

REQUIRED OUTPUT:
{props.schema}
