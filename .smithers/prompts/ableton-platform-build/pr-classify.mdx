# PR classification

Classify every PR #1 review and CI item as actionable, resolved, duplicate, informational, unsafe, or human-blocked. Preserve immutable IDs and URLs.

REQUIRED OUTPUT:
{props.schema}
