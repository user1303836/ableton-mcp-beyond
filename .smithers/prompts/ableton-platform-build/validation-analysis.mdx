# Validation analysis

Classify raw validation evidence by category, isolate unrelated baseline failures, and decide whether another bounded iteration is required. A missing check is not a pass.

REQUIRED OUTPUT:
{props.schema}
